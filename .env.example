# =============================================================================
# .ENV.EXAMPLE - Environment Variables Template
# =============================================================================
# Copy this file to .env and fill in your actual values.
# Never commit the .env file to version control!
#
# Required for the multi-agent data analyst to work:
# - OPENAI_API_KEY: Your OpenAI API key for LLM calls
# - SERVICE_ACCOUNT_PATH: Path to your Google Service Account JSON key file
# - DRIVE_FOLDER_ID: The ID of your Google Drive folder containing spreadsheets
# =============================================================================

# -----------------------------------------------------------------------------
# OPENAI API KEY
# -----------------------------------------------------------------------------
# Get your API key from: https://platform.openai.com/api-keys
# Used by: mcp_sheets_agent (ReAct agent), intent_agent, code_writer_agent, 
#          narrative_agent, critic_agent
# 
# IMPORTANT: Use GPT-4 or GPT-4o for best tool-calling performance.
# GPT-3.5 may not work reliably with complex tool-calling scenarios.
# -----------------------------------------------------------------------------
OPENAI_API_KEY=sk-your_openai_api_key_here

# -----------------------------------------------------------------------------
# OPENAI API BASE URL (Optional)
# -----------------------------------------------------------------------------
# Use this to connect to OpenAI-compatible APIs (Azure OpenAI, local LLMs, etc.)
# Default: https://api.openai.com/v1
# -----------------------------------------------------------------------------
BASE_URL=https://api.openai.com/v1

# -----------------------------------------------------------------------------
# OPENAI MODEL (Optional)
# -----------------------------------------------------------------------------
# The model to use for all agents. Default: gpt-4o
# Other options: gpt-4, gpt-4-turbo, gpt-3.5-turbo (not recommended)
# -----------------------------------------------------------------------------
OPENAI_MODEL=gpt-4o

# -----------------------------------------------------------------------------
# GOOGLE SHEETS MCP SERVER CONFIGURATION
# -----------------------------------------------------------------------------
# The MCP agent connects to a locally running mcp-google-sheets server
# using stdio transport. The server authenticates with Google using a
# Service Account, which provides API access to Google Sheets and Drive.
#
# SERVICE_ACCOUNT_PATH:
#   Path to the JSON key file downloaded from Google Cloud Console.
#   This file contains the credentials for your Google Service Account.
#
#   How to get it:
#   1. Go to console.cloud.google.com
#   2. Create a new project (or select existing)
#   3. Enable Google Sheets API and Google Drive API
#   4. Go to IAM & Admin > Service Accounts
#   5. Create a Service Account
#   6. Click on the service account, go to Keys tab
#   7. Add Key > Create new key > JSON
#   8. Download the JSON file and place it at the path below
#
# DRIVE_FOLDER_ID:
#   The ID of the Google Drive folder containing your spreadsheets.
#   This is found at the end of a Google Drive folder URL:
#   https://drive.google.com/drive/folders/THIS_PART_IS_THE_FOLDER_ID
#
#   IMPORTANT: You must share this folder with the service account email
#   (found in the JSON key file as "client_email") with Editor access.
# -----------------------------------------------------------------------------

# Path to the JSON key file (relative to project root or absolute path)
SERVICE_ACCOUNT_PATH=./credentials/service_account.json

# The folder ID from your Google Drive folder URL
DRIVE_FOLDER_ID=your_google_drive_folder_id_here
